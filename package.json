{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose -f docker-compose.yml -f docker-compose.data.yml -f docker-compose.gpu.yml -f docker-compose.api.yml up"
  },
  "dependencies": {}
}
